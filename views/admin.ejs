<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Berat Can Ulu</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: #f8fafc;
            color: #1e293b;
        }

        .admin-header {
            background: #1e3a8a;
            color: white;
            padding: 1.5rem 0;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .admin-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .admin-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .admin-subtitle {
            opacity: 0.9;
            font-weight: 300;
        }

        /* Admin Sidebar */
        .admin-sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            height: 100vh;
            background: white;
            box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
            z-index: 1000;
        }

        .admin-sidebar-header {
            background: #1e3a8a;
            color: white;
            padding: 1.5rem;
            text-align: center;
        }

        .admin-sidebar-header h3 {
            margin: 0;
            font-size: 1.2rem;
            font-weight: 600;
        }

        .admin-sidebar-menu {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .admin-sidebar-menu li {
            border-bottom: 1px solid #e2e8f0;
        }

        .admin-sidebar-menu a {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem 1.5rem;
            color: #374151;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .admin-sidebar-menu a:hover {
            background: #f8fafc;
            color: #2563eb;
        }

        .admin-sidebar-menu i {
            width: 20px;
            text-align: center;
            color: #64748b;
        }

        .admin-sidebar-menu a:hover i {
            color: #2563eb;
        }

        .welcome-section {
            background: white;
            border-radius: 12px;
            padding: 3rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            border: 1px solid #e2e8f0;
            text-align: center;
            margin-left: 250px;
            margin-bottom: 2rem;
        }

        .welcome-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: #1e293b;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
        }

        .welcome-title i {
            color: #2563eb;
        }

        .welcome-text {
            font-size: 1.1rem;
            color: #64748b;
            line-height: 1.6;
        }

        .admin-card {
            background: white;
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            border: 1px solid #e2e8f0;
        }

        .card-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #1e293b;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .card-title i {
            color: #2563eb;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat-item {
            text-align: center;
            padding: 1rem;
            background: #f8fafc;
            border-radius: 8px;
            border: 1px solid #e2e8f0;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            color: #2563eb;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: #64748b;
            font-size: 0.9rem;
        }

        .admin-actions {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .admin-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn-primary {
            background: #2563eb;
            color: white;
        }

        .btn-primary:hover {
            background: #1d4ed8;
            transform: translateY(-2px);
        }

        .btn-secondary {
            background: #64748b;
            color: white;
        }

        .btn-secondary:hover {
            background: #475569;
            transform: translateY(-2px);
        }

        .btn-success {
            background: #059669;
            color: white;
        }

        .btn-success:hover {
            background: #047857;
            transform: translateY(-2px);
        }

        .btn-danger {
            background: #dc2626;
            color: white;
        }

        .btn-danger:hover {
            background: #b91c1c;
            transform: translateY(-2px);
        }

        .messages-section {
            background: white;
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            border: 1px solid #e2e8f0;
        }

        .message-item {
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 1rem;
            margin-bottom: 1rem;
            background: #f8fafc;
        }

        .message-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .message-sender {
            font-weight: 600;
            color: #1e293b;
        }

        .message-date {
            color: #64748b;
            font-size: 0.9rem;
        }

        .message-content {
            color: #475569;
            line-height: 1.6;
        }

        .message-actions {
            display: flex;
            gap: 0.5rem;
            margin-top: 1rem;
        }

        .back-to-site {
            text-align: center;
            margin-top: 2rem;
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-left: 250px;
        }

        .back-to-site a {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem 1.5rem;
            background: #1e3a8a;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .back-to-site a:hover {
            background: #1e40af;
            transform: translateY(-2px);
        }

        .logout-btn {
            background: #dc2626 !important;
        }

        .logout-btn:hover {
            background: #b91c1c !important;
        }

        @media (max-width: 768px) {
            .admin-content {
                grid-template-columns: 1fr;
            }
            
            .admin-nav ul {
                flex-direction: column;
                gap: 1rem;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Admin Header -->
    <header class="admin-header">
        <div class="admin-container">
            <h1 class="admin-title">Admin Panel</h1>
            <p class="admin-subtitle">Berat Can Ulu Website Yönetimi</p>
        </div>
    </header>

    <!-- Admin Sidebar -->
    <div class="admin-sidebar">
        <div class="admin-sidebar-header">
            <h3>Admin Panel</h3>
        </div>
        <ul class="admin-sidebar-menu">
            <li><a href="/admin-messages"><i class="fas fa-envelope"></i> Mesajlar</a></li>
            <li><a href="/admin-settings"><i class="fas fa-cog"></i> Ayarlar</a></li>
        </ul>
    </div>

    <!-- Admin Content -->
    <div class="admin-container">
        <!-- Welcome Message -->
        <div class="welcome-section">
            <h2 class="welcome-title">
                <i class="fas fa-home"></i>
                Admin Paneline Hoş Geldiniz
            </h2>
            <p class="welcome-text">
                Sol taraftaki sidebar'dan istediğiniz bölüme erişebilirsiniz.
            </p>
        </div>

        <!-- Back to Site -->
        <div class="back-to-site">
            <a href="/">
                <i class="fas fa-arrow-left"></i>
                Siteye Dön
            </a>
            <a href="#" class="logout-btn" id="logoutBtn">
                <i class="fas fa-sign-out-alt"></i>
                Çıkış Yap
            </a>
        </div>
    </div>

    <script>
        // Sidebar navigation functionality
        document.querySelectorAll('.admin-sidebar-menu a').forEach(link => {
            link.addEventListener('click', function() {
                // Remove active class from all links
                document.querySelectorAll('.admin-sidebar-menu a').forEach(l => l.classList.remove('active'));
                
                // Add active class to clicked link
                this.classList.add('active');
            });
        });

        // Logout functionality
        document.getElementById('logoutBtn').addEventListener('click', function(e) {
            e.preventDefault();
            
            if (confirm('Çıkış yapmak istediğinizden emin misiniz?')) {
                // Clear admin session
                localStorage.removeItem('adminLoggedIn');
                localStorage.removeItem('adminUsername');
                
                // Redirect to login page
                window.location.href = '/login';
            }
        });

        // Check if user is logged in
        window.addEventListener('load', function() {
            const isLoggedIn = localStorage.getItem('adminLoggedIn');
            if (!isLoggedIn) {
                // Redirect to login if not authenticated
                window.location.href = '/login';
            }
        });
    </script>
</body>
</html> 